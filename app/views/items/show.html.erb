<p id="notice"><%= notice %></p>

<h2><%= @item.name %></h2>

<ul>
  <li><%= @item.quantity %> <%= @item.unit %></li>
  <li><%= @item.description %></li>
</ul>

<%= render 'quantity' %>

<%= link_to 'Back to items', items_path, class: 'btn btn-large btn-info', method: :get %>
 |
 <%= link_to 'Edit', edit_item_path(@item), class: 'btn btn-large btn-warning' %>