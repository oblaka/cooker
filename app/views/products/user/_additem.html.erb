<h2>Add item:</h2>
<%= bootstrap_form_for([@product, @product.items.build], layout: :inline) do |f| %>
    
    <%= f.hidden_field :product_id, value: @product.id %>
    <%= f.text_field :quantity, append: @product.unit  %> 
    <%= f.submit "Добавить" %>

<% end %>