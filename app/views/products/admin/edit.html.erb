<h1>Editing product</h1>

<%= render 'form' %>

<% back = 'Посмотреть на ' + @product.name %>
<%= link_to back, @product, class: 'btn btn-large btn-info' %> |
<%= link_to 'Back to products', products_path, class: 'btn btn-large btn-info', method: :get %>