<%= bootstrap_form_tag url: produce_recipe_path, layout: :inline do |f| %>


    <%= f.select "count", options_for_select((1..3).step(1)), hide_label: true %>
    <%= f.submit "Приготовить", class: "btn btn-success" %>

    <% end %>

    <%= @avale %>